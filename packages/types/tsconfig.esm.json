{
  "extends": "../../tsconfig.build.json",

  "compilerOptions": {
    "outDir": "./lib/esm"
  },

  "include": [
    "src/**/*.ts", "src/**/*.js"
  ],

  "exclude": [
    "coverage",
    "**/*.spec.ts",
  ]
}
