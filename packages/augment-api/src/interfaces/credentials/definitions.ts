import { rpc } from '@kiltprotocol/type-definitions'

/*
  This module contains all the extra RPC definitions for the credentials endpoint.
*/

/* eslint-disable */
export default {
  // types: {} must always be specified for the compilation to finish successfully.
  // The actual types are all exported from `./extraDefs`
  types: {},
  rpc: rpc.credentials
}
